---
slug: cicd
title: Add to CI/CD
---

import useBaseUrl from '@docusaurus/useBaseUrl';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

export const Grid = ({logos}) => {
    const items = logos.map((e, i) => {
      return (<CIBox key={i} {...e}/>)
    })
    return (
      <div
        className="ci-grid"
        >
        {items}
      </div>
    )
};

export const CIBox = ({image, link, maxHeight, newWindow, text}) => (
  <a
    className="ci-box"
    href={link}
    >
    <img
      src={image}
      target={newWindow ? "_blank" : null}
      style={{
        margin: "0 auto",
        maxHeight: maxHeight ?? "2rem"
      }}
      />
      {text && (<div className="ci-subtitle">{text}</div>)}
  </a>
)

Use one of our many integrations so your engineering team can see cost estimates in pull requests **before making changes**. This provides your team with a safety net as people can discuss costs as part of the workflow.

<img src={useBaseUrl("img/screenshots/actions-pull-request.png")} alt="Infracost pull request comment" width="90%" />

If you run into any issues, please join our [community Slack channel](https://www.infracost.io/community-chat), we'll help you very quickly 😄

## Source control integrations (recommended)

Infracost supports direct integration with GitHub and GitLab.
We recommend these integrations as they are much simpler to setup, and faster to run. [This page](/docs/guides/source_control_benefits/) explains more about the benefits of source control integrations over CI/CD integrations.
<Grid logos={[
    {
      image: "/docs/img/services/github.svg",
      text: "GitHub.com, Enterprise Cloud & Enterprise Server",
      link: "/docs/integrations/github_app",
      newWindow: false
    },
    {
      image: "/docs/img/services/gitlab.svg",
      text: "GitLab.com & self-managed",
      link: "/docs/integrations/gitlab_app",
      newWindow: true
    },
]}/>

## CI/CD integrations

The following integrations can also be used to post pull request comments.

<Grid logos={[
    {
      image: "/docs/img/services/github-actions.png",
      link: "https://github.com/infracost/actions",
      newWindow: true
    },
    {
      image: "/docs/img/services/gitlab-ci.png",
      link: "https://gitlab.com/infracost/infracost-gitlab-ci",
      newWindow: true
    },
    {
      image: "/docs/img/services/atlantis.png",
      link: "https://github.com/infracost/infracost-atlantis/",
      newWindow: true
    },
    {
      image: "/docs/img/services/azure-pipelines.png",
      link: "https://marketplace.visualstudio.com/items?itemName=Infracost.infracost-tasks",
      newWindow: true
    },
    {
      image: "/docs/img/services/jenkins.svg",
      link: "https://github.com/infracost/infracost-jenkins/",
      newWindow: true
    },
    {
      image: "/docs/img/services/bitbucket-pipelines.png",
      link: "https://bitbucket.org/infracost/infracost-bitbucket-pipeline/src/master/",
      newWindow: true
    },
    {
      image: "/docs/img/services/circleci.svg",
      link: "https://github.com/infracost/infracost-circleci",
      newWindow: true
    },
    {
      link: "/docs/integrations/generic_cicd",
      text: "Other CI/CD integrations using Docker or CLI",
      newWindow: false
    },
]}/>

## Third-party integrations

Infracost can also be used alongside many other [third-party systems](/docs/integrations/third_party_integrations/) including Terraform Cloud/Enterprise, Env0, Scalr and Spacelift. Note that these integrations are very minimal, do not post pull request comments or work with other Infracost Cloud features such as [Guardrails](/docs/infracost_cloud/guardrails/) or [Tagging policies](/docs/infracost_cloud/tagging_policies/).
