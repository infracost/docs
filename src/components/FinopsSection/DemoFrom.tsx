import React from "react";

const DemoForm = () => {
  const hello = "world";

  return (
    <form onSubmit={() => null} id="finops-form">
      <input />
    </form>
  );
};

export default DemoForm;
